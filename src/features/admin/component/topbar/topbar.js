
import { Typography } from '@mui/material'
import React from 'react'

const Topbar = () => {
    return (
        <Typography variant='h3' mb={2} >Admin Dashboard</Typography>
    )
}

export default Topbar