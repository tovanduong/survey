import { Box } from '@mui/system'
import React from 'react'

const Loading = () => {
    return (
        <Box padding={20}>
            <h1>Loading</h1>
        </Box>
    )
}

export default Loading