import { Typography } from '@mui/material'
import { Box } from '@mui/system'
import React from 'react'

const Loading = () => {
    return (
        <Box paddingLeft={80} pt={40}>
            <Typography variant='h1' >Loading ...</Typography>
        </Box>
    )
}

export default Loading